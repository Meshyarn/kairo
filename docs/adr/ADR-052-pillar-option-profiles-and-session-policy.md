# ADR-052: Pillar Option Profiles + Session Policy

Archived: The full ADR is now stored outside the public docs tree. This stub remains to preserve references.

Summary:
- Added `profile/sources/safety/trace` as top-level options to reduce option overload.
- Introduced session policy persistence and reuse via `sessionId`.
- Standardized option resolution priority and trace output for debugging.
